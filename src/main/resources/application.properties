spring.application.name=secure-mail-analyzer-backend

spring.datasource.url=jdbc:postgresql://localhost:5433/secure_mail_db
spring.datasource.username=securemail
spring.datasource.password=securemail123
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

jwt.secret=${JWT_SECRET:secure-mail-analyzer-jwt-secret-key-for-dev-only-please-change-123456}
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# Frontend geliştirme portları (React/Vite/Angular)
cors.allowed-origins=http://localhost:3000,http://localhost:5173,http://localhost:4200