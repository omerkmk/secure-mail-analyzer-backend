# PVC = kalıcı disk talebi
# Pod ölse bile veritabanı verisi kaybolmasın (Compose'daki postgres_data volume)
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
